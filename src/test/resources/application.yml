# Property encryption configuration
jasypt:
  encryptor:
    password: encryptPasswordPlaceHolder

# Mailing configuration - SimpleJavaMail
# http://www.simplejavamail.org/#/configuration
simplejavamail:
  transportstrategy: SMTP_TLS
  smtp:
    host: smtp.gmail.com
    port: 587
    username: mevengar@gmail.com
    password: mailPasswordPlaceHolder
  defaults:
    from: 
      name: Packt Notifier
      address: mevengar@gmail.com
    to:
      # Additional addresses can be added using semicolon (;)
      address: mevengar@gmail.com

# Notifications enabled/disabled configuration
notifications:
  email:
   enabled: false
  twitter:
   enabled: true
   
# Twitter OAuth configuration
twitter:
  accessSecret: accessSecretPlaceHolder
  accessToken: accessTokenPlaceHolder
  consumerKey:  consumerKeyPlaceholder
  consumerSecret: consumerSecretPlaceholder

  
# Service confguration
service:
  cron: 0 0 4 * * ?
  #cron: 0 * * * * * # For testing purposes
  #proxy:
    # Do not add with 'http://' at the beginning!
    #host:
    #port:
    

api-config:
  max-number-days: 3

